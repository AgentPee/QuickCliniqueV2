@model QuickClinique.Models.StudentRegisterViewModel
@{
    ViewData["Title"] = "Student Registration";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Registration - QuickClinic</title>
    <link rel="stylesheet" href="~/css/studReg.css" asp-append-version="true" />
</head>
<body>
    <div class="container">
        <div class="side-panel">
            <div class="logo">
                <img src="~/img/logo.png" alt="QuickClinic Logo" class="logo-img">
            </div>
            <h1 class="clinic-name">QuickClinique</h1>
            <p class="tagline">Your Health, Our Priority</p>
            <div class="medical-icons">
                <div class="floating-icon">💊</div>
                <div class="floating-icon">🩺</div>
                <div class="floating-icon">❤️</div>
                <div class="floating-icon">🏥</div>
                <div class="floating-icon">🔬</div>
                <div class="floating-icon">🩹</div>
            </div>
        </div>

        <div class="form-panel">
            <div class="form-container">
                <h2 class="form-title">Create Account</h2>
                <p class="form-subtitle">Join QuickClinique to manage your health</p>

                @if (TempData["SuccessMessage"] != null)
                {
                    <div class="success-message">
                        @TempData["SuccessMessage"]
                    </div>
                }

                <form asp-action="Register" method="post" enctype="multipart/form-data" class="auth-form">
                    <div asp-validation-summary="ModelOnly" class="error-message"></div>

                    <div class="input-group">
                        <label asp-for="Idnumber" class="control-label">ID Number</label>
                        <input asp-for="Idnumber" class="form-control" maxlength="8" pattern="[0-9]{8}" required placeholder="Enter 8-digit ID" />
                        <span asp-validation-for="Idnumber" class="text-danger"></span>
                        <span class="input-icon">🆔</span>
                    </div>

                    <div class="form-row">
                        <div class="input-group">
                            <label asp-for="FirstName" class="control-label">First Name</label>
                            <input asp-for="FirstName" class="form-control" required />
                            <span asp-validation-for="FirstName" class="text-danger"></span>
                            <span class="input-icon">👤</span>
                        </div>

                        <div class="input-group">
                            <label asp-for="LastName" class="control-label">Last Name</label>
                            <input asp-for="LastName" class="form-control" required />
                            <span asp-validation-for="LastName" class="text-danger"></span>
                            <span class="input-icon">👤</span>
                        </div>
                    </div>

                    <div class="input-group">
                        <label asp-for="Email" class="control-label">Email</label>
                        <input asp-for="Email" class="form-control" type="email" required placeholder="Enter your email" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                        <span class="input-icon">📧</span>
                    </div>

                    <div class="input-group">
                        <label asp-for="PhoneNumber" class="control-label">Phone Number</label>
                        <input asp-for="PhoneNumber" class="form-control"
                               placeholder="e.g., 09345672824"
                               pattern="09[0-9]{9}"
                               title="Phone number must start with 09 and be 11 digits"
                               required />
                        <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                        <span class="input-icon">📱</span>
                        <small class="input-hint">Must start with 09 and be 11 digits total</small>
                    </div>

                    <div class="form-row">
                        <div class="input-group">
                            <label asp-for="Gender" class="control-label">Gender</label>
                            <select asp-for="Gender" class="form-control" required>
                                <option value="">Select Gender</option>
                                <option value="Male">Male</option>
                                <option value="Female">Female</option>
                                <option value="Other">Other</option>
                            </select>
                            <span asp-validation-for="Gender" class="text-danger"></span>
                            <span class="input-icon">⚧️</span>
                        </div>

                        <div class="input-group">
                            <label asp-for="Birthdate" class="control-label">Birthdate</label>
                            <input asp-for="Birthdate" class="form-control" type="date" required />
                            <span asp-validation-for="Birthdate" class="text-danger"></span>
                            <span class="input-icon">📅</span>
                        </div>
                    </div>

                    <div class="input-group">
                        <label class="control-label">Student ID Image (Front)</label>
                        <div class="file-input-container">
                            <!-- Hidden main input for form submission -->
                            <input asp-for="StudentIdImageFront" id="StudentIdImageFront" class="form-control file-input" type="file" accept="image/*" required style="display: none;" />
                            <!-- Hidden input for file upload (no capture) -->
                            <input type="file" id="StudentIdImageFrontUpload" accept="image/*" style="display: none;" />
                            <!-- Hidden input for camera (with capture) -->
                            <input type="file" id="StudentIdImageFrontCamera" accept="image/*" capture="environment" style="display: none;" />
                            <div class="file-input-buttons">
                                <button type="button" class="btn-upload" onclick="openFileUpload('StudentIdImageFront')">
                                    📤 Upload File
                                </button>
                                <button type="button" class="btn-camera" onclick="openCamera('StudentIdImageFront')">
                                    📷 Take Photo
                                </button>
                            </div>
                            <div id="preview-front" class="image-preview"></div>
                            <span asp-validation-for="StudentIdImageFront" class="text-danger"></span>
                        </div>
                        <small class="input-hint">Upload or take a photo of the front of your student ID</small>
                    </div>

                    <div class="input-group">
                        <label class="control-label">Student ID Image (Back)</label>
                        <div class="file-input-container">
                            <!-- Hidden main input for form submission -->
                            <input asp-for="StudentIdImageBack" id="StudentIdImageBack" class="form-control file-input" type="file" accept="image/*" required style="display: none;" />
                            <!-- Hidden input for file upload (no capture) -->
                            <input type="file" id="StudentIdImageBackUpload" accept="image/*" style="display: none;" />
                            <!-- Hidden input for camera (with capture) -->
                            <input type="file" id="StudentIdImageBackCamera" accept="image/*" capture="environment" style="display: none;" />
                            <div class="file-input-buttons">
                                <button type="button" class="btn-upload" onclick="openFileUpload('StudentIdImageBack')">
                                    📤 Upload File
                                </button>
                                <button type="button" class="btn-camera" onclick="openCamera('StudentIdImageBack')">
                                    📷 Take Photo
                                </button>
                            </div>
                            <div id="preview-back" class="image-preview"></div>
                            <span asp-validation-for="StudentIdImageBack" class="text-danger"></span>
                        </div>
                        <small class="input-hint">Upload or take a photo of the back of your student ID</small>
                    </div>

                    <div class="input-group">
                        <label asp-for="Password" class="control-label">Password</label>
                        <input asp-for="Password" class="form-control" type="password" required />
                        <span asp-validation-for="Password" class="text-danger"></span>

                        <span class="toggle-password" onclick="togglePassword('Password')">👁️</span>
                    </div>

                    <div class="input-group">
                        <label asp-for="ConfirmPassword" class="control-label">Confirm Password</label>
                        <input asp-for="ConfirmPassword" class="form-control" type="password" required />
                        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>

                        <span class="toggle-password" onclick="togglePassword('ConfirmPassword')">👁️</span>
                    </div>

                    <button type="submit" class="submit-btn">
                        <span class="btn-text">Create Account</span>
                        <span class="btn-loader"></span>
                    </button>
                </form>

                <div class="back-to-login">
                    <p>Already have an account? <a asp-action="Login" class="back-link">Login here</a></p>
                </div>
            </div>
        </div>
    </div>

    <script src="~/js/studReg.js"></script>
</body>
</html>